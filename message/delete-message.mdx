---
title: "Deletar mensagens"
api: "DELETE /instances/{instanceId}/token/{token}/messages"
description: "Apaga uma mensagem enviada ou recebida em um chat"
---
import InstanceId from '/snippets/pt/params/instance-id.mdx'
import Token from '/snippets/pt/params/token.mdx'


## Conceituação

Método utilizado para apagar uma mensagem em um chat, você pode deletar tanto uma mensagem que enviou quanto uma mensagem enviada por um contato, para utilizar este recurso você só vai precisar do messageId da mensagem que quer apagar.

#### Exemplo no WhatsApp
<div style={{ display: "flex", justifyContent: "center" }}>
  <img src="/images/delete-message.jpeg" width="400" />
</div>

---

## Atributos

### Header

<InstanceId />
<Token />
### Query Parameters

<ParamField query="messageId" type="string" required>
  ID original da mensagem
</ParamField>

<ParamField query="phone" type="string" required>
  Telefone (ou ID do grupo para casos de envio para grupos) do destinatário/remetente no formato DDI DDD NÚMERO. Ex: 551199999999
</ParamField>

<ParamField query="owner" type="boolean" required>
  Informe `true` caso você tenha enviado a mensagem ou `false` para casos onde seja uma mensagem recebida
</ParamField>

<ParamField query="deleteForMe" type="boolean">
  Informe `true` para apagar a mensagem apenas para você ("Apagar para mim"), mantendo-a visível para o destinatário. Caso não seja informado ou seja `false`, a mensagem será apagada para todos.
</ParamField>

## Exemplo de requisição

### URL exemplo

```bash
DELETE https://api.z-api.io/instances/{instanceId}/token/{instanceToken}/messages?messageId=123&phone=5511999998888&owner=true
```

### Excluindo apenas para mim

Para excluir a mensagem apenas "para mim", basta adicionar o parâmetro `deleteForMe` na requisição do endpoint de deletar mensagem.

```bash
DELETE https://api.z-api.io/instances/{instanceId}/token/{instanceToken}/messages?messageId=123&phone=5511999998888&owner=true&deleteForMe=true
```

---

## Response

### 204

No content

```json
{}
```

### 405

Neste caso certifique que esteja enviando corretamente a especificação do método, ou seja verifique se você enviou o DELETE conforme especificado no início deste tópico.

### 415

Caso você receba um erro 415, certifique de adicionar na headers da requisição o "Content-Type" do objeto que você está enviando, em sua grande maioria "application/json".