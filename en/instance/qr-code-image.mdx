---
title: "QRCode as image"
api: "GET /instances/{instanceId}/token/{token}/qr-code/image"
description: "Returns the QRCode in base64 image format"
---
import InstanceId from '/snippets/en/params/instance-id.mdx'
import Token from '/snippets/en/params/token.mdx'


## Overview

Returns the QRCode in base64 image format, so you can display it in an image component.

---

## Attributes

### Header

<InstanceId />
<Token />
---

## Response

### 200

<Warning>
The QR Code image may be returned in Base64, or, on devices that require the **[Passkey](/en/tips/extension-guide)**, the **Challenge** object to continue the authentication process.
</Warning>

**Example response for devices that require the [Passkey](/en/tips/extension-guide)**
```json
{
  "challenge": {
    "allowCredentials": [],
    "challenge": "V275tRcRUu2XjHiF9a6GTKFaZj8G4W_EEuRwdW5zCAM",
    "extensions": {
      "uvm": true
    },
    "rpId": "whatsapp.com",
    "timeout": 600000,
    "userVerification": "required"
  }
}
```

In cases where this response is returned, it is necessary to complete the authentication requested via the Challenge. For this, we have the **[Complete the Challenge](/en/instance/passkey-prologue)** endpoint.

### 405

Make sure you are correctly sending the method specification, that is, verify that you sent POST or GET as specified at the beginning of this topic.
