---
title: "QRCode em imagem"
api: "GET /instances/{instanceId}/token/{token}/qr-code/image"
description: "Retorna o QRCode em formato de imagem base64"
---
import InstanceId from '/snippets/pt/params/instance-id.mdx'
import Token from '/snippets/pt/params/token.mdx'


## Conceituação

Retorna o QRCode em formato de imagem base64, para que você exiba em um componente de imagem.

---

## Atributos

### Header

<InstanceId />
<Token />
---

## Response

### 200

<Warning>
Pode ser retornada a imagem do QR Code em Base64 ou, em dispositivos que solicitam a **[Chave de Acesso](/tips/extension-guide)**, o objeto **Challenge** para dar continuidade ao processo de autenticação.
</Warning>

**Exemplo de retorno para dispositivos que precisam da [Chave de Acesso](/tips/extension-guide)**
```json
{
  "challenge": {
    "allowCredentials": [],
    "challenge": "V275tRcRUu2XjHiF9a6GTKFaZj8G4W_EEuRwdW5zCAM",
    "extensions": {
      "uvm": true
    },
    "rpId": "whatsapp.com",
    "timeout": 600000,
    "userVerification": "required"
  }
}
```

Em casos desse retorno, é necessário concluir a autenticação que foi solicitada por meio do Challenge, para isso temos o endpoint para **[Concluir o Challenge](/instance/passkey-prologue)**

### 405

Neste caso certifique que esteja enviando corretamente a especificação do método, ou seja verifique se você enviou o POST ou GET conforme especificado no início deste tópico.
